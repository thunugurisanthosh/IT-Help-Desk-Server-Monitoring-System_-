# Architecture

Place the project architecture diagram here as:

`architecture.png`

Recommended flow:

Administrator PC
→ AWS VPC
→ LINUX01 (Apache + Node Exporter)
→ MONITOR01 (Prometheus + Grafana)
